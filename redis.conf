# Redis configuration file

# Disable protected mode
protected-mode no

# Bind to all network interfaces
bind 0.0.0.0

# Set the port number
port 6379

# Set the database file location (commented out by default)
# dbfilename dump.rdb
